// Prepared lab only; never connects to the Survival server.
import mineflayer from 'mineflayer';import pf from 'mineflayer-pathfinder';import {installBoat} from './boat.mjs';import http from 'node:http';
const bot=mineflayer.createBot({host:'127.0.0.1',port:25577,username:'BoatLab',version:'1.16.5'});bot.loadPlugin(pf.pathfinder);const boat=installBoat(bot);bot.on('physicsTick',()=>{if(!bot.vehicle&&bot.entity?.isInWater)bot.setControlState('jump',true);});
http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://localhost');let result;if(u.pathname==='/place')result=await boat.place();if(u.pathname==='/drive')result=await boat.drive({x:Number(u.searchParams.get('x')),z:Number(u.searchParams.get('z'))});if(u.pathname==='/dismount')result=await boat.dismount();res.end(JSON.stringify({result,p:bot.entity?.position,vehicle:bot.vehicle?.position,health:bot.health,inventory:bot.inventory.items().map(i=>[i.name,i.count])}));}catch(e){res.statusCode=500;res.end(e.stack);}}).listen(3094,'127.0.0.1');
