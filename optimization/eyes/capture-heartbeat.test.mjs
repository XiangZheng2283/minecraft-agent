import test from 'node:test';import assert from 'node:assert/strict';import {captureHeartbeatGuard} from '../../native-mirror.mjs';
test('Brief heartbeat replacement does not report a disconnected recorder',()=>{let time=10000,missing=false;const health=captureHeartbeatGuard(()=>{if(missing)throw Error('ENOENT');return{time:10000,frames:20,encoderAlive:true};},()=>time);assert.equal(health(),true);missing=true;time=11000;assert.equal(health(),true);time=19000;assert.equal(health(),false);});
test('Stopped or never-started encoder cannot report recording progress',()=>{const health=captureHeartbeatGuard(()=>({time:10000,frames:0,encoderAlive:false}),()=>10000);assert.equal(health(),false);});
test('Fresh frames restore recording health',()=>{let time=10000,stamp=10000;const health=captureHeartbeatGuard(()=>({time:stamp,frames:50,encoderAlive:true}),()=>time);assert.equal(health(),true);time=20000;assert.equal(health(),false);stamp=time;assert.equal(health(),true);});
