import test from 'node:test';import assert from 'node:assert/strict';import {Vec3} from 'vec3';import {defenseActions} from '../../hostile-defense.mjs';
function fixture(health){let attacks=0,raised=false;const e={id:1,name:'skeleton',height:1.9,position:new Vec3(2,70,0)},bot={health,entity:{position:new Vec3(0,70,0)},entities:{1:e},world:{raycast:()=>null},inventory:{slots:{45:{name:'shield'}}},pathfinder:{setGoal:()=>{}},clearControlStates:()=>{},equip:async()=>{},lookAt:async()=>{},activateItem:()=>{raised=true;},deactivateItem:()=>{raised=false;},attack:()=>{attacks++;},waitForTicks:async()=>{bot.health=7;}};let fn;defenseActions(bot,(k,d,f)=>{if(k==='defend_1')fn=f;},{item:()=>({name:'iron_sword'})});return{bot,run:()=>fn(),attacks:()=>attacks,raised:()=>raised};}
test('Defense aborts attacks below cutoff and keeps shield between decisions',async()=>{const f=fixture(7);await f.run();assert.equal(f.attacks(),0);assert.equal(f.raised(),true);});
test('Damage during shield wait stops the next attack',async()=>{const f=fixture(10);await f.run();assert.equal(f.attacks(),0);assert.equal(f.raised(),true);});
