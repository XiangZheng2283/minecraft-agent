import test from 'node:test';import assert from 'node:assert/strict';import {Vec3} from 'vec3';import {waitForBarter} from '../../covered-barter.mjs';
test('A brute that arrives during a trade stops the wait and raises the shield',async()=>{let ticks=0,shield=false,aim=false;const b={setControlState(){},entity:{position:new Vec3(0,88,0)},entities:{},inventory:{slots:{45:{name:'shield'}}},pathfinder:{setGoal(){}},clearControlStates(){},activateItem(off){shield=off},async lookAt(){aim=true},async waitForTicks(){if(++ticks===12)b.entities[9]={id:9,name:'piglin_brute',metadata:{8:50},position:new Vec3(5,88,0)}}};assert.equal(await waitForBarter(b,125),false);assert.equal(ticks,12);assert.equal(b.barterThreatId,9);assert.ok(shield&&aim)});
test('A brute on a separate lower floor does not interrupt a trade',async()=>{let ticks=0;const b={setControlState(){},entity:{position:new Vec3(0,88,0)},entities:{9:{id:9,name:'piglin_brute',metadata:{8:50},position:new Vec3(1,80,0)}},async waitForTicks(){ticks++}};assert.equal(await waitForBarter(b,125),true);assert.equal(ticks,125)});
